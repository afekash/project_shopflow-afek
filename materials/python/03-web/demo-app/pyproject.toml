[project]
name = "fastapi-demo"
version = "0.1.0"
description = "FastAPI demo for the Web APIs lesson"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
]

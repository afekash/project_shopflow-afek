# john-bryce

Data Engineering Course Materials

## Setup

```bash
uv sync
uv run python generate_notebooks.py
```

This generates interactive Jupyter notebooks from the markdown source files. Open the `.ipynb` files in Cursor or Jupyter to run code cells.